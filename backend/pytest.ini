[pytest]
testpaths = tests
markers =
    integration: marks tests as requiring a running Postgres database (deselect with -m "not integration")
addopts = -v --tb=short -m "not integration"
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
